# Tetris Game

This repository contains a simple terminal-based Tetris game implemented in Python using the `curses` module.

## Running the Game

Make sure you have Python 3 installed. To start the game, run:

```bash
python tetris.py
```

Use the arrow keys to move and rotate the pieces. Press `q` to quit.
